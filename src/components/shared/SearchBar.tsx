import { SearchIcon } from 'lucide-react'
import React from 'react'

const SearchBar = () => {
  return (
    <div>
        <SearchIcon size={20} className='hover:text-darkRed hoverEffect cursor-pointer'/>
    </div>
  )
}

export default SearchBar